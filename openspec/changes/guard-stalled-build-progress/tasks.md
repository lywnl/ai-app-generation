## 1. 诊断与回合状态

- [x] 1.1 实现类型化 BuildBlockDiagnostic 和 BuildGateDecision，替换构建工具文案前缀分支并覆盖所有有效在线计划/代码无修改拒绝；依赖：规划评审；验证：诊断分类、完整阻塞集合、规范化限长和原构建门禁协议测试，不消费真实构建次数。
- [x] 1.2 实现 BuildProgressGuard 与回合归属/清理；依赖：1.1；验证：第一次/第二次/送达后第三次、去重、严格子集进展、替换阻塞不清零、无计划创建、代码revision、关闭及新回合、fail-open不算模型进展的单测。

## 2. 提交与请求边界

- [x] 2.1 在词法工具作用域采集调用级诊断及成功工具后的可信快照，接入 GuardedToolExecution 可选内部元数据和在线Facade；依赖：1.1、1.2；验证：真实隔离计划/文件工具写回、旧二参构造和非Vue/评测兼容、取消与结果元数据归属、外部协议字段不变。
- [x] 2.2 流控制器在可靠工具结果提交后观察并按当前代次认领停滞终止，停止批次剩余真实工具；依赖：2.1；验证：持久化失败不观察、重复结果只计一次、REPLAN_PENDING无旁路、旧回调及取消后不推进，最后拒绝先于终态发布。
- [x] 2.3 接入反馈票据、临时消息合并与有效回调确认；依赖：1.2、2.2；验证：同批请求不提前终止、仅领取/门禁准备/压缩不算送达、SDK同步回调与无回调抛错、恢复路径不丢票据、进展后旧票据失效，消息不入记忆。

## 3. 收口和兼容

- [x] 3.1 新增内部BUILD_STALLED并接入FAILED、稳定正文、记忆投影及终态预算；依赖：2.2；验证：计划和代码原因准确、真实构建次数不变、不伪造协议/构建失败、不刷新预览、长路径摘要满足预算。
- [x] 3.2 覆盖生命周期竞争和前端终态兼容；依赖：2.3、3.1；验证：停滞/取消/超时/删除只产生唯一合法终态，下一回合重新计数；前端会话FAILED显示原因且不刷新，SSE类型及字段保持原样。

## 4. 联合验证

- [x] 4.1 运行诊断、保护器、构建工具、构建会话、控制器、响应处理器、请求编排、终态、取消、SSE及前端会话回归；依赖：3.2；验证：Maven和Vitest真实退出码及报告，隔离目录下无skip通过，不调用真实模型/数据库。
- [x] 4.2 同步规格检查、改动范围及真实旧计划哈希核对，保存结构化证据；依赖：4.1；验证：OpenSpec严格校验、git diff --check、真实计划未变，已有三次构建和其他保护无不相关变更。

## 5. 交付条件

strict 独立规划评审及 start-apply 在实现前完成；独立代码评审和 verify 在任务证据完备后完成。不自动归档、推送、重启，旧报告不冒充当前验收。
