# 实现验收

- 变更：guard-stalled-build-progress；等级 strict；方式 machine；日期 2026-09-21。
- 结论：`Ready for Archive`。九项任务完成，独立规划及代码评审PASS，无未解决阻塞。未自动归档、提交、推送或重启。
- 代码基线：master/6f4fb4d 之后本变更的未提交差异和新增文件，最终以机器指纹为准，不只使用Git HEAD。

## 覆盖与结果

结构化映射见 `verification-manifest.json`：5项Requirement（包含对应Scenario）和9项任务，共14条。正式新规格与增量全文一致，已有软Replan的修改Requirement也一致。原归档、旧变更报告不重写，不以旧验收证明本次语义。

- 门禁诊断覆盖无/空计划、文件和依赖未完成、待修订、回合异常以及代码无修改拒绝；类型和规范化集合供内部判断，受限message供展示。
- 计划进展要求成功工具前后及相对最近拒绝的阻塞集合严格减少；无计划合法创建和代码真实变更序号解除单独处理。读取、无变化、纯版本、无关变更、阻塞替换及fail-open不能单独清零。
- 调用级ScopedValue元数据不进入外部工具JSON；只有可靠提交、PROVIDED且当前回合有效才观察。真实文件写回、构建会话计数、取消及不相关调用隔离有测试覆盖。
- 普通续行和两类恢复携票据；SDK启动动作绑定代次，可信正文/合法普通完成/合法工具执行确认送达，准备/压缩/无响应启动失败不确认。旧代次、旧票据和同批请求不能提前耗尽纠偏机会。
- 第一次说明、第二次安排纠偏、有效送达后无进展再拒绝触发内部BUILD_STALLED。最后拒绝和批次跳过信号经实际延迟发布队列先于终态；剩余真实工具和后续请求不执行。
- 最终FAILED且不刷新预览，正文和记忆含受限原因与实际构建次数；1200码点上限纳入终态reserve。回合关闭清理保护器，新回合清零；取消/超时/删除的唯一终态机制保持。

## 执行证据

所有路径位于项目 `.codex/guard-stalled-build-progress/`；完整命令及工作目录在结构化映射与 apply-evidence.md。

| 验证 | 结果 | 原始记录 |
| --- | --- | --- |
| 后端28个相关测试类 | 529项通过，0失败/错误/跳过 | logs/19-full-green.log |
| 前端全套Vitest | 21文件282项通过 | logs/20-frontend-all.log |
| 前端TypeScript检查 | 退出码0 | logs/21-frontend-types.log |
| 规格同步、旧计划、样本和差异检查 | 8份旧计划哈希未变，样本一致，diff检查通过 | logs/22-scope-final.json |
| OpenSpec变更与正式规范严格校验 | 均通过 | logs/23-openspec-change.log、logs/24-openspec-specs.log |

测试中的文件、计划、作用域、会话、流控制器和发布队列是真实执行；模型供应商、聊天持久化等外部依赖为受控替身。Maven fork JVM 的user.dir/tmpdir均在本变更隔离目录，选定JUnit原始报告保存于junit/。没有调用真实模型、数据库、发布服务或真实网站npm构建，不宣称已证明模型一定理解提示或消除所有循环。

## 独立评审与失败处置

`reports/independent-code-review.md`：独立上下文 `/root/planning_review` 未编写实现，最终PASS。发现并闭合了两项时序缺陷：原始违规响应过早确认送达、BUILD_STALLED未排入统一披露队列。对应有效反馈、恢复携票据和延迟队列回归均纳入最终529项。

早期编译遗漏内部枚举分支、测试跨包访问及新测试输入/取消断言问题已修正，详见实施索引；未放宽生产生命周期或关闭协议检查。18号完整回归唯一错误是隔离目录缺失既有共享JSON样本，复制原样本后19号相同集合通过，未切回真实项目输出目录。

## 剩余边界

- 提示送达只证明请求及可信响应的服务端事实，不证明模型理解。任意其他循环仍由原总预算和截止机制处理。
- 代码修改相关性不新增语义推断，沿用真实revision和真实构建三次上限；保留原依赖/基础设施重试。
- 普通前端代码和布局无修改，仅增加FAILED契约测试；不新增SSE或工具字段、数据表及查询接口。
- 运行服务未重启，旧真实计划不自动迁移；新代码需下次启动后生效。
