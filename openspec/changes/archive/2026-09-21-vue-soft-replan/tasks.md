## 1. 计划状态与路径边界（依赖：无；落点：`ai/plan/`、`PlanStoragePathResolver`、`AppPlanStateManager`）

- [x] 1.1 新增计划状态模型、计划文件条目和修订记录，覆盖计划标识、版本、回合标识、模式、文件动作与计划级状态；通过模型序列化/反序列化测试验证字段完整性。
- [x] 1.2 实现计划状态管理，支持按应用加载上一版本、在当前有效生成租约下绑定新的活动 `turnId`、版本校验、修订合并、历史长度限制和计划上下文摘要；通过新回合接管、过期回合覆盖和缺失计划测试。
- [x] 1.3 实现 `.plan.json` 的原子保存和安全路径解析；通过进程中断模拟或临时文件残留测试确认不会留下半份 JSON。
- [x] 1.4 将 `.plan.json` 加入普通项目路径的受保护集合，并覆盖目录遍历、读取、写入、修改、删除和下载排除路径；通过文件工具安全测试确认模型无法访问计划文件。

## 2. 计划工具与受信协议（依赖：1.1-1.4；落点：`MakePlanTool`、`UpdatePlanTool`、计划协议与 `VueToolExecutionFact`）

- [x] 2.1 实现独立的计划工具结果模型和严格协议解析，覆盖成功、拒绝、版本冲突和失败结果；通过未知字段、缺失字段和错误版本测试确认协议边界。
- [x] 2.2 实现 `makePlan`，只允许在线 `MUTATION_REQUIRED` 作用域创建 `FULL` 计划，并校验文件动作、依赖、计划版本和当前 `turnId`；通过首轮创建、只读拒绝、评测不可用和取消拒绝测试。
- [x] 2.3 实现 `updatePlan`，校验修订原因、版本和依赖，递增版本并记录修订历史；通过成功修订、版本冲突、空原因和取消期间写入测试。
- [x] 2.4 将 `makePlan`、`updatePlan` 接入在线工具白名单、工具管理、可信执行事实解析和工具展示链路；通过工具发现、成功结果可解析和工具消息可展示测试。
- [x] 2.5 保持文件工具 8 字段协议和现有状态枚举不变；通过文件正常结果、资源限制结果和终态协议回归测试确认没有新增计划字段。

## 3. 回合状态与软 Replan 反馈（依赖：1、2；落点：`VueTurnContext`、`ReplanContext`、`StreamingRequestController`、`AiServiceStreamingResponseHandler`）

- [x] 3.1 将 `ReplanContext` 绑定到 `VueTurnContext` 和当前 `turnId`，保存待处理状态、触发标识、证据、版本和反馈次数；通过不同回合隔离、取消清理和并发访问测试。
- [x] 3.2 实现基于可信工具事实和当前计划的偏差检测，覆盖计划外路径、目标文件持续不存在、重复失败和依赖不成立；通过每类触发条件、原始事实观察顺序、版本切换后的计数重置及重复触发去重测试。
- [x] 3.3 在 `StreamingRequestController` 增加计划偏差观察与一次性反馈领取路径；通过工具完成后反馈只被下一次模型请求领取一次的时序测试。
- [x] 3.4 将反馈作为临时 `SystemMessage` 注入下一次模型请求，不写入 `UserMessage` 或普通聊天历史，并包含先调用 `updatePlan` 和不得复述内部提示的约束；通过初始请求、普通续请求和 incomplete recovery 请求的记忆持久化、计划摘要合并及反馈不包含服务端标记的测试。2026-09-21 追加删除输出检测后，不再要求运行时扫描模型输出。
- [x] 3.5 在反馈达到上限后执行 fail-open，解除本轮写入阻断并记录诊断状态；通过超限后可继续写入且不重复注入反馈的测试。

## 4. 文件变更阻断与构建闸门（依赖：1-3；落点：`FileToolExecutionScopeManager`、`BuildProjectTool`、构建状态管理）

- [x] 4.1 扩展现有文件变更拒绝策略，在 `replanPending` 时对写入、修改、删除返回标准 `REJECTED` 结果，并保持读取工具和 `updatePlan` 可用；通过协议字段集合、无实际文件变更和读取放行测试。
- [x] 4.2 在 `updatePlan` 成功后解除文件变更阻断，并保留新的计划版本和证据；通过修订前拒绝、修订后允许和版本递增测试。
- [x] 4.3 在 `buildProject` 消耗构建尝试前增加计划状态检查：`KEEP` 不要求变更，`CREATE`/`MODIFY`/`DELETE` 必须有当前计划版本下的可信成功变更记录，依赖未满足、`OUT_OF_PLAN` 和待处理 Replan 必须拒绝；通过拒绝时不增加构建次数、依赖链阻断和计划修订后重新计算待处理文件的测试。
- [x] 4.4 保持现有构建失败分类、反思提示、构建次数和最终终态协议，仅补充计划假设错误时调用 `updatePlan` 的提示；通过第 1/2/3 次构建失败和成功构建回归测试。

## 5. 模式隔离与提示词展示（依赖：2-4；落点：`VueToolNames`、Vue 在线提示词、现有工具事件展示链路）

- [x] 5.1 修改 Vue 在线系统提示词，明确首轮调用 `makePlan`、偏差反馈优先、修订前不得继续变更和构建前计划约束；通过提示词契约测试确认评测提示词未被修改。
- [x] 5.2 确认 HTML、MULTI_FILE、`READ_ONLY` 和 EVALUATION 路径不得调用计划工具或计划闸门；在线工具集合中的计划工具在非 `MUTATION_REQUIRED` 作用域必须返回受信拒绝；通过模式路由、作用域和工具白名单测试。
- [x] 5.3 让计划工具结果沿用现有工具执行事件和前端 Markdown/工具消息展示，不新增 SSE 类型或 REST 接口；通过在线流事件快照和前端已有工具渲染测试。

## 6. 集成验证与交付检查（依赖：1-5；落点：相关 Java 测试、前端测试和 `.codex/vue-soft-replan/` 证据）

- [x] 6.1 增加在线软 Replan 集成测试：创建计划、执行文件工具、触发偏差、领取临时反馈、调用 `updatePlan` 并继续当前 generation；验证没有创建第二个 `TokenStream` 或最终终态，并覆盖计划外事实观察顺序。
- [x] 6.2 增加计划文件安全与回合生命周期集成测试：验证路径保护、版本冲突、旧回合覆盖、取消提交竞态、租约失效、只读和评测隔离。
- [x] 6.3 增加构建闸门集成测试：无计划或计划未完成时拒绝 `buildProject`，依赖未满足时拒绝，计划通过后沿用现有构建协议，并确认 `.plan.json` 不进入下载包。
- [x] 6.4 运行项目相关 Java 单元/集成测试和前端聚焦测试，记录失败路径、协议兼容性和软 Replan 证据；通过 OpenSpec 规定的真实验证命令后再标记任务完成。

## 7. Strict 规划修订后的补充任务

以下任务依赖 1-6 的原有结构，专门落实本次规划评审发现的边界；完成后才可重新登记 strict 实施基线。

- [x] 7.1 在 `AiCodeGeneratorFacade.generateVueProjectStream` 的受信回合初始化阶段接管已有计划，并将计划摘要合并到初始、普通续请求和 incomplete recovery 的临时系统消息；依赖：1.2、3.4；验证：回合开始时序测试和三类请求快照。
- [x] 7.2 为计划保存和成功文件事实写回接入 `VueBuildLease.commitWhileActive(Supplier<T>)` 原子边界，并让 CAS 同时校验版本和活动 `turnId`；使用 `activeTurnId` 作为接管令牌，不新增绑定代次字段；依赖：1.2、1.3、2.2、2.3；验证：取消先到/提交先到、旧回合覆盖测试。
- [x] 7.3 在计划路径访问中使用不跟随符号链接的安全目录边界，能力不支持时 fail-closed，覆盖父目录替换、目标链接和临时文件残留；依赖：1.3、1.4；验证：安全目录竞态和原子替换测试。
- [x] 7.4 在创建/修订计划时校验悬空、自依赖和循环依赖，在构建闸门和检测器中按当前版本判断依赖是否完成；依赖：2.2、2.3、3.2、4.3；验证：A→B→C、删除 B、重写 B、A→B→A 输入输出测试。
- [x] 7.5 调整可信文件事实观察顺序，在计划状态归档前触发计划外偏差，并在计划版本切换后重置失败计数和已处理触发标识；依赖：3.2、4.1；验证：原始结果观察顺序和版本切换去重测试。
- [x] 7.6 写回 `PlanStatus.BUILT`，在偏差、`updatePlan`、fail-open 和新回合加载时维护 `REPLAN_PENDING`，合并 recovery 反馈，拒绝空计划并在成功变更或计划修订后重置失败计数；依赖：4.3、4.4、6.1；验证：成功构建、跨回合偏差、fail-open、空计划、恢复请求和计数清零测试。
- [x] 7.7 对 7.1-7.6 运行聚焦 Java/前端验证并保留真实原始日志；依赖：7.1-7.6；验证：命令退出码、日志和差异核对。

## 8. 流程交付条件

以下是 review/apply/verify 的阶段性交付条件，不属于实现任务，不作为进入评审或验收的循环前置：

- 规划修订完成后，按 strict 要求完成独立规划评审，并由 `record-plan` 绑定新的规划、持续约束、参考代码和评审报告。
- 首次业务代码修改前，通过 `start-apply` 核验 strict 规划指纹并登记实施开始。
- 7.7 和全部必需实现任务完成后，运行 `openspec-verify-change`，重建 `reports/verification-manifest.json` 和 `reports/verification.md`，并取得独立代码评审。

## 9. 内部输出检测物理删除（2026-09-21 用户追加确认）

- [x] 9.1 删除专用检测器、策略 API、恢复协调器和工具参数一致性扫描；保留最终工具参数归一化与普通协议错误，验证标记及参数差异不再触发已删除检测。
- [x] 9.2 将统一信号队列与安全策略解耦，保留回调串行、批量发布、终态排序、监听器失败和取消边界；改写混合测试保留原有业务断言。
- [x] 9.3 删除普通生成、Vue 转录及取消/删除/失败收尾中的专用标记封口；保留合成记忆协议和唯一持久化，验证普通取消仍为 CANCELLED。
- [x] 9.4 删除专用后端消息和前端状态/事件分支，保留正文、工具参数增量、可信工具展示、计划展示及其他恢复事件；前端测试、类型检查与构建通过。
- [x] 9.5 完成干净构建、后端全量及 P/E 聚焦验证，记录删除清单、保留边界、非本次失败与独立代码评审结论。证据见 reports/internal-output-removal-evidence.md；全量保留四项基础设施配置失败，不宣称全绿。
