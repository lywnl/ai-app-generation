# 分层记忆架构 · 一期实现计划(L0 + L1 + 冷启动)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把"满 100 条硬截断"的记忆升级为"L1 语义摘要 + L0 热窗口"的分层装饰器——长对话不失忆、冷启动不断裂、token 可控。

**Architecture:** `LayeredChatMemory`(装饰器)包裹现有 `MessageWindowChatMemory`;`messages()` 返回前前置 L1 摘要;异步 `MemorySummaryService` 用 `deepseek-v4-flash` 滚动维护摘要存 MySQL;游标 `lastSummarizedId` 协调,异步线程只读 `chat_history`、只写 `app_memory_summary`,不碰 Redis/delegate。

**Tech Stack:** Java 17/21, Spring Boot 3, LangChain4j, MyBatis-Flex, Hutool, Caffeine, JUnit 5 + Mockito, MySQL, Redis.

**Spec:** `.claude/plans/2026-05-30-分层记忆架构设计.md`

**运行测试:** `mvn -q test -Dtest=<TestClass>`(项目用 Maven)

---

## File Structure

**新建:**
| 文件 | 职责 |
|------|------|
| `model/entity/AppMemorySummary.java` | L1 摘要实体(每 app 一行) |
| `mapper/AppMemorySummaryMapper.java` | MyBatis-Flex Mapper |
| `config/MemorySummarizationExecutorConfig.java` | 后台摘要线程池 |
| `ai/memory/MemorySummaryPromptBuilder.java` | 5 段摘要 prompt 构建(纯函数) |
| `ai/memory/LayeredChatMemory.java` | ChatMemory 装饰器 |
| `service/MemorySummaryService.java` | 摘要服务接口 |
| `service/impl/MemorySummaryServiceImpl.java` | 异步提炼 + 游标 + single-flight + 降级 |
| `sql/schema.sql`(追加) | `app_memory_summary` 建表 |

**修改:**
| 文件 | 改动 |
|------|------|
| `service/ChatHistoryService.java` | 放宽 `loadChatHistoryToMemory` 签名为 `ChatMemory`;新增按游标查询 |
| `service/impl/ChatHistoryServiceImpl.java` | 同上实现 |
| `mapper/ChatHistoryMapper.java` | 新增"查 id > cursor 的消息"方法(或用 QueryWrapper) |
| `ai/AiGeneratorServiceFactory.java` | 用 `LayeredChatMemory` 包裹现有 `MessageWindowChatMemory` |
| `core/handler/StreamHandlerExecutor.java` | `@Resource` 注入 `MemorySummaryService`,透传给两个 handle |
| `core/handler/SimpleTextStreamHandler.java` | `handle()` 加参数;`doOnComplete` 后触发异步提炼 |
| `core/handler/JsonMessageStreamHandler.java` | 同上 |

**测试:**
- `ai/memory/LayeredChatMemoryTest.java`、`MemorySummaryPromptBuilderTest.java`
- `service/impl/MemorySummaryServiceImplTest.java`

---

## Task 0: Spike — 验证装饰器不破坏工具循环消息顺序

**目的:** 验证方案 Z 的**消息序列契约**——`messages()` 前置摘要对后,序列满足 user/ai 交替(API 不拒)、且 `MessageWindowChatMemory` 的 tool 对成对驱逐为 LangChain4j 内置。注:这是**单元层契约验证**;"工具循环端到端不破"的真正验证落在 Task 7 Step 3 冒烟 + Task 9 集成测试。

**Files:**
- Test: `src/test/java/com/lyw/appgeneration/ai/memory/InjectionSpikeTest.java`

- [ ] **Step 1: 写 spike 测试**——验证"前置 [UserMessage 摘要 + AiMessage 确认] 对"后,消息序列满足 user/ai 交替(不出现连续同角色)

```java
package com.lyw.appgeneration.ai.memory;

import dev.langchain4j.data.message.*;
import dev.langchain4j.memory.chat.MessageWindowChatMemory;
import org.junit.jupiter.api.Test;
import java.util.*;
import static org.junit.jupiter.api.Assertions.*;

class InjectionSpikeTest {

    /** 验证:前置一对 (User摘要, AI确认) 后,整个序列不出现连续同角色 —— DeepSeek/OpenAI 兼容要求。 */
    @Test
    void prependSummaryPairKeepsAlternation() {
        MessageWindowChatMemory delegate = MessageWindowChatMemory.withMaxMessages(100);
        delegate.add(UserMessage.from("第1轮:做一个待办App"));
        delegate.add(AiMessage.from("好的,已生成"));

        String summary = "# 应用目标\n待办清单App";
        List<ChatMessage> result = new ArrayList<>();
        result.add(UserMessage.from("以下是早期对话摘要,供参考:\n" + summary));
        result.add(AiMessage.from("明白,我会基于摘要继续。"));
        result.addAll(delegate.messages());

        // 断言:相邻消息角色不相同(允许 system 例外,这里无 system)
        for (int i = 1; i < result.size(); i++) {
            assertNotEquals(result.get(i - 1).type(), result.get(i).type(),
                "位置 " + i + " 出现连续同角色,会被 API 拒绝");
        }
    }

    /** 验证:LangChain4j MessageWindowChatMemory 内置 tool 对成对驱逐(驱逐 AiMessage 含 toolReq 时连带驱逐 toolResult)。 */
    @Test
    void messageWindowEvictsToolPairTogether() {
        MessageWindowChatMemory mem = MessageWindowChatMemory.withMaxMessages(3);
        mem.add(UserMessage.from("u1"));
        ToolExecutionRequest req = ToolExecutionRequest.builder().id("t1").name("read").arguments("{}").build();
        mem.add(AiMessage.from(req));
        mem.add(ToolExecutionResultMessage.from(req, "result"));
        mem.add(UserMessage.from("u2")); // 触发驱逐,超过 maxMessages=3

        // 断言:不存在 orphan ToolExecutionResultMessage(其对应的 AiMessage 已被驱逐则它也应被驱逐)
        boolean hasOrphanToolResult = mem.messages().stream()
            .anyMatch(m -> m instanceof ToolExecutionResultMessage)
            && mem.messages().stream().noneMatch(m -> m instanceof AiMessage am && am.hasToolExecutionRequests());
        assertFalse(hasOrphanToolResult, "存在 orphan ToolExecutionResultMessage,LangChain4j 未成对驱逐");
    }
}
```

- [ ] **Step 2: 运行 spike**

Run: `mvn -q test -Dtest=InjectionSpikeTest`
Expected: 两个测试均 PASS。若 `messageWindowEvictsToolPairTogether` FAIL → 说明内置驱逐行为与预期不符,需在 Task 5 增加显式保护(回退讨论)。

- [ ] **Step 3: 记录结论**——在本 plan Task 5 标注采用的注入形式(默认 User+AI 对)。Spike 测试可保留或删除。

- [ ] **Step 4: Commit**

```bash
git add src/test/java/com/lyw/appgeneration/ai/memory/InjectionSpikeTest.java
git commit -m "test: spike 验证分层记忆装饰器的消息注入与 tool 对驱逐"
```

---

## Task 1: `app_memory_summary` 表 + Entity + Mapper

**Files:**
- Modify: `prod/sql/schema.sql`(及本地 `src/main/resources` 对应建表脚本,按项目现状)
- Create: `model/entity/AppMemorySummary.java`
- Create: `mapper/AppMemorySummaryMapper.java`

- [ ] **Step 1: 追加建表 SQL**(参照现有 `chat_history` 风格)

```sql
CREATE TABLE IF NOT EXISTS app_memory_summary (
    id                BIGINT       PRIMARY KEY COMMENT '主键(snowflake)',
    appId             BIGINT       NOT NULL COMMENT '应用ID',
    summary           MEDIUMTEXT   COMMENT '5段模板摘要内容',
    lastSummarizedId  BIGINT       NOT NULL DEFAULT 0 COMMENT '已覆盖到的 chat_history.id 游标',
    summaryTokens     INT          NOT NULL DEFAULT 0 COMMENT '摘要估算token',
    failCount         INT          NOT NULL DEFAULT 0 COMMENT '连续失败计数(circuit breaker)',
    createTime        DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updateTime        DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    isDelete          TINYINT      NOT NULL DEFAULT 0,
    UNIQUE KEY uk_appId (appId)
) COMMENT 'L1 滚动摘要(每app一行)';
```

- [ ] **Step 2: 写 Entity**(MyBatis-Flex + Lombok,对齐 `ChatHistory.java` 风格)

```java
package com.lyw.appgeneration.model.entity;

import com.mybatisflex.annotation.*;
import com.mybatisflex.core.keygen.KeyGenerators;
import lombok.*;
import java.io.Serial;
import java.io.Serializable;
import java.time.LocalDateTime;

/** L1 滚动摘要实体。@author lyw */
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
@Table("app_memory_summary")
public class AppMemorySummary implements Serializable {
    @Serial private static final long serialVersionUID = 1L;

    @Id(keyType = KeyType.Generator, value = KeyGenerators.snowFlakeId)
    private Long id;

    @Column("appId")
    private Long appId;

    private String summary;

    @Column("lastSummarizedId")
    private Long lastSummarizedId;

    @Column("summaryTokens")
    private Integer summaryTokens;

    @Column("failCount")
    private Integer failCount;

    @Column("createTime")
    private LocalDateTime createTime;

    @Column("updateTime")
    private LocalDateTime updateTime;

    @Column(value = "isDelete", isLogicDelete = true)
    private Integer isDelete;
}
```

- [ ] **Step 3: 写 Mapper**(对齐 `ChatHistoryMapper.java`)

```java
package com.lyw.appgeneration.mapper;

import com.mybatisflex.core.BaseMapper;
import com.lyw.appgeneration.model.entity.AppMemorySummary;

/** @author lyw */
public interface AppMemorySummaryMapper extends BaseMapper<AppMemorySummary> {
}
```

- [ ] **Step 4: 编译验证**

Run: `mvn -q compile`
Expected: BUILD SUCCESS

- [ ] **Step 5: Commit**

```bash
git add src/main/java/com/lyw/appgeneration/model/entity/AppMemorySummary.java \
        src/main/java/com/lyw/appgeneration/mapper/AppMemorySummaryMapper.java prod/sql/schema.sql
git commit -m "feat: 新增 app_memory_summary 表/实体/Mapper(L1摘要持久层)"
```

---

## Task 2: 摘要线程池配置

**Files:**
- Create: `config/MemorySummarizationExecutorConfig.java`(对齐 `ImageCollectionExecutorConfig`)

- [ ] **Step 1: 写线程池配置**

```java
package com.lyw.appgeneration.config;

import cn.hutool.core.thread.ExecutorBuilder;
import cn.hutool.core.thread.ThreadFactoryBuilder;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import java.util.concurrent.ExecutorService;
import java.util.concurrent.LinkedBlockingQueue;
import java.util.concurrent.ThreadPoolExecutor;

/** 后台记忆摘要线程池。@author lyw */
@Configuration
public class MemorySummarizationExecutorConfig {

    @Bean(name = "memorySummarizationExecutor", destroyMethod = "shutdown")
    public ExecutorService memorySummarizationExecutor() {
        return ExecutorBuilder.create()
                .setCorePoolSize(5)
                .setMaxPoolSize(10)
                .setWorkQueue(new LinkedBlockingQueue<>(200))
                .setThreadFactory(ThreadFactoryBuilder.create().setNamePrefix("Memory-Sum-").build())
                // 队列满直接丢弃:摘要是 best-effort,下轮会再触发
                .setHandler(new ThreadPoolExecutor.DiscardPolicy())
                .build();
    }
}
```

- [ ] **Step 2: 编译验证**

Run: `mvn -q compile`
Expected: BUILD SUCCESS

- [ ] **Step 3: Commit**

```bash
git add src/main/java/com/lyw/appgeneration/config/MemorySummarizationExecutorConfig.java
git commit -m "feat: 新增 memorySummarizationExecutor 后台摘要线程池"
```

---

## Task 3: 摘要 Prompt 构建器(5 段模板,纯函数)

**Files:**
- Create: `ai/memory/MemorySummaryPromptBuilder.java`
- Test: `src/test/java/com/lyw/appgeneration/ai/memory/MemorySummaryPromptBuilderTest.java`

- [ ] **Step 1: 写失败测试**

```java
package com.lyw.appgeneration.ai.memory;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class MemorySummaryPromptBuilderTest {

    @Test
    void buildContainsFiveSectionsAndOldSummaryAndNewMessages() {
        String prompt = MemorySummaryPromptBuilder.build("# 应用目标与定位\n旧摘要内容", "用户:把按钮改成蓝色\nAI:已修改");
        assertTrue(prompt.contains("应用目标与定位"));
        assertTrue(prompt.contains("用户偏好与硬约束"));
        assertTrue(prompt.contains("已否决的方案"));
        assertTrue(prompt.contains("关键设计决策与理由"));
        assertTrue(prompt.contains("当前进度速览"));
        assertTrue(prompt.contains("旧摘要内容"), "应包含旧摘要用于滚动合并");
        assertTrue(prompt.contains("把按钮改成蓝色"), "应包含新增对话");
    }

    @Test
    void buildWithBlankOldSummaryStillValid() {
        String prompt = MemorySummaryPromptBuilder.build("", "用户:做个博客");
        assertTrue(prompt.contains("做个博客"));
        assertNotNull(prompt);
    }
}
```

- [ ] **Step 2: 运行验证失败**

Run: `mvn -q test -Dtest=MemorySummaryPromptBuilderTest`
Expected: FAIL(`MemorySummaryPromptBuilder` 不存在 / 编译错误)

- [ ] **Step 3: 写实现**

```java
package com.lyw.appgeneration.ai.memory;

import cn.hutool.core.util.StrUtil;

/**
 * 构建 L1 滚动摘要的 prompt(5 段模板)。
 * 借鉴 Claude Code SessionMemory:固定段落防发散,优先保"应用目标"与"用户偏好与纠正"。
 * @author lyw
 */
public final class MemorySummaryPromptBuilder {

    private MemorySummaryPromptBuilder() {}

    /** 整段摘要 token 预算(约,对齐 RAG max-context-chars:4000)。 */
    public static final int MAX_SUMMARY_TOKENS = 1800;

    private static final String TEMPLATE = """
            你是对话摘要助手。请基于【旧摘要】和【新增对话】,产出更新后的摘要。
            只记录"从当前代码状态推导不出来"的信息(已生成的代码不要复述)。
            严格使用下面 5 个固定段落,只更新内容,不要新增段落:

            # 应用目标与定位
            (用户要做什么应用、核心定位)
            # 用户偏好与硬约束
            (明确偏好,以及"不要X/要Y"的纠正 —— 含否定项,最重要)
            # 已否决的方案
            (试过但被用户否决的,防止重复犯错)
            # 关键设计决策与理由
            (记理由,不记代码结果)
            # 当前进度速览
            (一两句,指向 vue_project_<appId> 最新代码;不复制代码)

            规则:
            - 增量合并:在旧摘要基础上吸收新增对话,不要丢失旧的关键约束。
            - 控制在 %d token 以内;超了优先精简"当前进度速览",保住"应用目标"和"用户偏好与硬约束"。
            - 直接输出 5 段摘要正文,不要解释、不要寒暄。

            【旧摘要】
            %s

            【新增对话】
            %s
            """;

    public static String build(String oldSummary, String newMessages) {
        String old = StrUtil.isBlank(oldSummary) ? "(无,首次生成)" : oldSummary;
        return String.format(TEMPLATE, MAX_SUMMARY_TOKENS, old, newMessages);
    }
}
```

- [ ] **Step 4: 运行验证通过**

Run: `mvn -q test -Dtest=MemorySummaryPromptBuilderTest`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/main/java/com/lyw/appgeneration/ai/memory/MemorySummaryPromptBuilder.java \
        src/test/java/com/lyw/appgeneration/ai/memory/MemorySummaryPromptBuilderTest.java
git commit -m "feat: 新增 5 段摘要 prompt 构建器 + 单测"
```

---

## Task 4: `MemorySummaryService`(异步提炼 + 游标 + single-flight + 降级)

**Files:**
- Create: `service/MemorySummaryService.java`
- Create: `service/impl/MemorySummaryServiceImpl.java`
- Modify: `mapper/ChatHistoryMapper.java`(新增按游标查询)+ `service/ChatHistoryService.java` / `impl`
- Test: `src/test/java/com/lyw/appgeneration/service/impl/MemorySummaryServiceImplTest.java`

- [ ] **Step 1: ChatHistoryService 新增"查 id > cursor 的消息(正序)"**

`ChatHistoryService.java` 接口新增:
```java
/** 查询 appId 下 id > cursorId 的消息,按 id 正序,最多 limit 条。供摘要增量提炼。 */
java.util.List<com.lyw.appgeneration.model.entity.ChatHistory>
        listMessagesAfterCursor(Long appId, Long cursorId, int limit);
```

`ChatHistoryServiceImpl.java` 实现:
```java
@Override
public List<ChatHistory> listMessagesAfterCursor(Long appId, Long cursorId, int limit) {
    QueryWrapper qw = QueryWrapper.create()
            .eq("appId", appId)
            .gt("id", cursorId == null ? 0L : cursorId)
            .orderBy("id", true)   // 正序:id 单调近似时间序
            .limit(limit);
    return this.list(qw);
}
```
> 游标用 `chat_history.id`(snowflake,单实例近似单调)。若未来多写库导致乱序,可改用 `createTime`+`id` 复合游标。

- [ ] **Step 2: 写服务接口**

```java
package com.lyw.appgeneration.service;

/** L1 滚动摘要服务。@author lyw */
public interface MemorySummaryService {

    /** 异步触发摘要提炼(钩子调用),内部 single-flight + best-effort。 */
    void triggerSummarizationAsync(Long appId);

    /** 读取当前摘要(messages() 拼接用);无则返回空串。 */
    String getCurrentSummary(Long appId);
}
```

- [ ] **Step 3: 写失败测试**(mock 模型与依赖,验证滚动/游标/降级/single-flight)

```java
package com.lyw.appgeneration.service.impl;

import com.lyw.appgeneration.mapper.AppMemorySummaryMapper;
import com.lyw.appgeneration.model.entity.AppMemorySummary;
import com.lyw.appgeneration.model.entity.ChatHistory;
import com.lyw.appgeneration.model.enums.ChatHistoryMessageTypeEnum;
import com.lyw.appgeneration.service.ChatHistoryService;
import dev.langchain4j.model.chat.ChatModel;
import dev.langchain4j.model.chat.response.ChatResponse;
import dev.langchain4j.data.message.AiMessage;
import org.junit.jupiter.api.*;
import org.mockito.*;
import java.util.*;
import static org.mockito.Mockito.*;
import static org.junit.jupiter.api.Assertions.*;

class MemorySummaryServiceImplTest {

    @Mock ChatHistoryService chatHistoryService;
    @Mock AppMemorySummaryMapper summaryMapper;
    @Mock ChatModel summarizationModel;
    MemorySummaryServiceImpl service;

    @BeforeEach void setup() {
        MockitoAnnotations.openMocks(this);
        // 直接同步执行的线程池,便于测试
        service = new MemorySummaryServiceImpl(chatHistoryService, summaryMapper, summarizationModel,
                java.util.concurrent.Executors.newSingleThreadExecutor());
    }

    private ChatHistory msg(long id, String type, String text) {
        return ChatHistory.builder().id(id).messageType(type).message(text).build();
    }

    @Test
    void summarizeRollsUpAndAdvancesCursor() throws Exception {
        when(summaryMapper.selectOneByQuery(any())).thenReturn(null); // 首次无摘要
        when(chatHistoryService.listMessagesAfterCursor(eq(1L), any(), anyInt()))
            .thenReturn(List.of(msg(10, "user", "做个待办App"), msg(11, "ai", "已生成")));
        when(summarizationModel.chat(anyString())).thenReturn("# 应用目标与定位\n待办App");

        service.summarizeNow(1L); // 暴露同步方法供测试

        ArgumentCaptor<AppMemorySummary> cap = ArgumentCaptor.forClass(AppMemorySummary.class);
        verify(summaryMapper).insert(cap.capture());
        assertEquals(11L, cap.getValue().getLastSummarizedId(), "游标应推进到最新消息id");
        assertTrue(cap.getValue().getSummary().contains("待办App"));
    }

    @Test
    void noNewMessagesSkips() {
        when(summaryMapper.selectOneByQuery(any())).thenReturn(
            AppMemorySummary.builder().appId(1L).lastSummarizedId(11L).summary("old").build());
        when(chatHistoryService.listMessagesAfterCursor(eq(1L), eq(11L), anyInt())).thenReturn(List.of());

        service.summarizeNow(1L);

        verify(summarizationModel, never()).chat(anyString());
        verify(summaryMapper, never()).update(any());
    }

    @Test
    void modelFailureDoesNotAdvanceCursorAndNotThrow() {
        when(summaryMapper.selectOneByQuery(any())).thenReturn(
            AppMemorySummary.builder().appId(1L).lastSummarizedId(5L).summary("old").failCount(0).build());
        when(chatHistoryService.listMessagesAfterCursor(eq(1L), eq(5L), anyInt()))
            .thenReturn(List.of(msg(6, "user", "x")));
        when(summarizationModel.chat(anyString())).thenThrow(new RuntimeException("model down"));

        assertDoesNotThrow(() -> service.summarizeNow(1L));
        // 游标未推进:update 时 lastSummarizedId 仍为 5(或仅 failCount++)
        ArgumentCaptor<AppMemorySummary> cap = ArgumentCaptor.forClass(AppMemorySummary.class);
        verify(summaryMapper).update(cap.capture());
        assertEquals(5L, cap.getValue().getLastSummarizedId());
        assertEquals(1, cap.getValue().getFailCount());
    }

    @Test
    void getCurrentSummaryReturnsEmptyWhenAbsent() {
        when(summaryMapper.selectOneByQuery(any())).thenReturn(null);
        assertEquals("", service.getCurrentSummary(1L));
    }
}
```

- [ ] **Step 4: 运行验证失败**

Run: `mvn -q test -Dtest=MemorySummaryServiceImplTest`
Expected: FAIL(`MemorySummaryServiceImpl` 不存在)

- [ ] **Step 5: 写实现**

```java
package com.lyw.appgeneration.service.impl;

import cn.hutool.core.collection.CollUtil;
import cn.hutool.core.util.StrUtil;
import com.lyw.appgeneration.ai.memory.MemorySummaryPromptBuilder;
import com.lyw.appgeneration.mapper.AppMemorySummaryMapper;
import com.lyw.appgeneration.model.entity.AppMemorySummary;
import com.lyw.appgeneration.model.entity.ChatHistory;
import com.lyw.appgeneration.model.enums.ChatHistoryMessageTypeEnum;
import com.lyw.appgeneration.service.ChatHistoryService;
import com.lyw.appgeneration.service.MemorySummaryService;
import com.mybatisflex.core.query.QueryWrapper;
import dev.langchain4j.model.chat.ChatModel;
import jakarta.annotation.Resource;
import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Qualifier;
import org.springframework.stereotype.Service;

import java.time.LocalDateTime;
import java.util.List;
import java.util.Set;
import java.util.concurrent.ConcurrentHashMap;
import java.util.concurrent.ExecutorService;

/**
 * L1 滚动摘要服务实现。
 * 触发:对话结束钩子异步调用;single-flight 防同 app 并发;失败 best-effort、游标不前进、circuit breaker。
 * @author lyw
 */
@Slf4j
@Service
public class MemorySummaryServiceImpl implements MemorySummaryService {

    /** 单次提炼最多并入的新消息条数(留 buffer 给 L0,容忍重叠)。 */
    private static final int MAX_MESSAGES_PER_RUN = 60;
    /** 触发提炼的最小新增条数(低于则跳过,避免高频小提炼)。 */
    private static final int MIN_NEW_MESSAGES_TO_SUMMARIZE = 8;
    /** circuit breaker:连续失败上限。 */
    private static final int MAX_FAIL = 3;

    private final ChatHistoryService chatHistoryService;
    private final AppMemorySummaryMapper summaryMapper;
    private final ChatModel summarizationModel;
    private final ExecutorService executor;

    /** single-flight:正在提炼的 appId。 */
    private final Set<Long> inFlight = ConcurrentHashMap.newKeySet();

    public MemorySummaryServiceImpl(ChatHistoryService chatHistoryService,
                                    AppMemorySummaryMapper summaryMapper,
                                    @Qualifier("openAiChatModel") ChatModel summarizationModel,
                                    @Qualifier("memorySummarizationExecutor") ExecutorService executor) {
        this.chatHistoryService = chatHistoryService;
        this.summaryMapper = summaryMapper;
        this.summarizationModel = summarizationModel;
        this.executor = executor;
    }

    @Override
    public void triggerSummarizationAsync(Long appId) {
        if (appId == null || appId <= 0) return;
        if (!inFlight.add(appId)) return;              // single-flight:已在提炼则跳过
        try {
            executor.submit(() -> {
                try { summarizeNow(appId); }
                finally { inFlight.remove(appId); }
            });
        } catch (Exception e) {                         // 线程池拒绝等
            inFlight.remove(appId);
            log.warn("提交摘要任务失败 appId={}: {}", appId, e.getMessage());
        }
    }

    /** 同步提炼一次(供测试与 triggerSummarizationAsync 内部调用)。best-effort,不抛异常。 */
    public void summarizeNow(Long appId) {
        try {
            AppMemorySummary current = summaryMapper.selectOneByQuery(
                    QueryWrapper.create().eq("appId", appId));
            if (current != null && current.getFailCount() != null && current.getFailCount() >= MAX_FAIL) {
                log.warn("appId={} 摘要连续失败 {} 次,circuit breaker 暂停", appId, current.getFailCount());
                return;
            }
            long cursor = current == null || current.getLastSummarizedId() == null ? 0L : current.getLastSummarizedId();
            List<ChatHistory> news = chatHistoryService.listMessagesAfterCursor(appId, cursor, MAX_MESSAGES_PER_RUN);
            if (CollUtil.isEmpty(news) || news.size() < MIN_NEW_MESSAGES_TO_SUMMARIZE) {
                return;                                 // 新增不足,跳过
            }
            String oldSummary = current == null ? "" : StrUtil.nullToEmpty(current.getSummary());
            String newText = renderMessages(news);
            String prompt = MemorySummaryPromptBuilder.build(oldSummary, newText);

            String newSummary;
            try {
                newSummary = summarizationModel.chat(prompt);
            } catch (Exception modelErr) {              // 模型失败:游标不前进,failCount++
                log.error("摘要模型调用失败 appId={}: {}", appId, modelErr.getMessage());
                bumpFail(appId, current);
                return;
            }

            long newCursor = news.get(news.size() - 1).getId();
            upsert(appId, current, newSummary, newCursor);
            log.info("摘要更新成功 appId={},游标 {} -> {}", appId, cursor, newCursor);
        } catch (Exception e) {                         // 兜底:绝不影响主流程
            log.error("摘要提炼异常 appId={}: {}", appId, e.getMessage(), e);
        }
    }

    @Override
    public String getCurrentSummary(Long appId) {
        try {
            AppMemorySummary s = summaryMapper.selectOneByQuery(QueryWrapper.create().eq("appId", appId));
            return s == null ? "" : StrUtil.nullToEmpty(s.getSummary());
        } catch (Exception e) {
            log.warn("读取摘要失败 appId={}: {}", appId, e.getMessage());
            return "";                                  // 降级:无摘要,只用 L0
        }
    }

    private String renderMessages(List<ChatHistory> list) {
        StringBuilder sb = new StringBuilder();
        for (ChatHistory h : list) {
            boolean isUser = ChatHistoryMessageTypeEnum.USER.getValue().equals(h.getMessageType());
            sb.append(isUser ? "用户:" : "AI:").append(StrUtil.maxLength(h.getMessage(), 4000)).append('\n');
        }
        return sb.toString();
    }

    private void upsert(Long appId, AppMemorySummary current, String summary, long newCursor) {
        int tokens = summary == null ? 0 : summary.length() / 4;
        if (current == null) {
            summaryMapper.insert(AppMemorySummary.builder()
                    .appId(appId).summary(summary).lastSummarizedId(newCursor)
                    .summaryTokens(tokens).failCount(0).build());
        } else {
            current.setSummary(summary);
            current.setLastSummarizedId(newCursor);
            current.setSummaryTokens(tokens);
            current.setFailCount(0);
            current.setUpdateTime(LocalDateTime.now());
            summaryMapper.update(current);
        }
    }

    private void bumpFail(Long appId, AppMemorySummary current) {
        if (current == null) {                          // 首次就失败:插一行只记 failCount
            summaryMapper.insert(AppMemorySummary.builder()
                    .appId(appId).summary("").lastSummarizedId(0L).summaryTokens(0).failCount(1).build());
        } else {
            current.setFailCount((current.getFailCount() == null ? 0 : current.getFailCount()) + 1);
            current.setUpdateTime(LocalDateTime.now());
            summaryMapper.update(current);              // 注意:不改 lastSummarizedId(游标不前进)
        }
    }
}
```

> 模型 Bean:复用现有 `openAiChatModel`(已是 `deepseek-v4-flash` 非流式)。如需独立配置(不同 maxTokens/temperature),后续新建 `summarizationChatModel` Bean 并改 `@Qualifier`。

- [ ] **Step 6: 运行验证通过**

Run: `mvn -q test -Dtest=MemorySummaryServiceImplTest`
Expected: PASS(4 个用例)

- [ ] **Step 7: Commit**

```bash
git add src/main/java/com/lyw/appgeneration/service/MemorySummaryService.java \
        src/main/java/com/lyw/appgeneration/service/impl/MemorySummaryServiceImpl.java \
        src/main/java/com/lyw/appgeneration/service/ChatHistoryService.java \
        src/main/java/com/lyw/appgeneration/service/impl/ChatHistoryServiceImpl.java \
        src/test/java/com/lyw/appgeneration/service/impl/MemorySummaryServiceImplTest.java
git commit -m "feat: MemorySummaryService 异步滚动摘要(游标+single-flight+降级)"
```

---

## Task 5: `LayeredChatMemory` 装饰器

**Files:**
- Create: `ai/memory/LayeredChatMemory.java`
- Test: `src/test/java/com/lyw/appgeneration/ai/memory/LayeredChatMemoryTest.java`

- [ ] **Step 1: 写失败测试**

```java
package com.lyw.appgeneration.ai.memory;

import com.lyw.appgeneration.service.MemorySummaryService;
import dev.langchain4j.data.message.*;
import dev.langchain4j.memory.chat.MessageWindowChatMemory;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;
import java.util.List;
import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;

class LayeredChatMemoryTest {

    @Test
    void messagesPrependsSummaryPairWhenPresent() {
        MessageWindowChatMemory delegate = MessageWindowChatMemory.builder().id(1L).maxMessages(100).build();
        delegate.add(UserMessage.from("做待办App"));
        delegate.add(AiMessage.from("已生成"));
        MemorySummaryService svc = mock(MemorySummaryService.class);
        when(svc.getCurrentSummary(1L)).thenReturn("# 应用目标与定位\n待办App");

        LayeredChatMemory mem = new LayeredChatMemory(delegate, svc);
        List<ChatMessage> msgs = mem.messages();

        assertTrue(msgs.size() >= 4);
        assertInstanceOf(UserMessage.class, msgs.get(0));            // 摘要 User
        assertInstanceOf(AiMessage.class, msgs.get(1));             // 确认 Ai
        assertTrue(((UserMessage) msgs.get(0)).singleText().contains("待办App"));
        // 交替性:无连续同角色
        for (int i = 1; i < msgs.size(); i++)
            assertNotEquals(msgs.get(i-1).type(), msgs.get(i).type());
    }

    @Test
    void messagesNoSummaryReturnsDelegateAsIs() {
        MessageWindowChatMemory delegate = MessageWindowChatMemory.builder().id(1L).maxMessages(100).build();
        delegate.add(UserMessage.from("hi"));
        MemorySummaryService svc = mock(MemorySummaryService.class);
        when(svc.getCurrentSummary(1L)).thenReturn("");

        LayeredChatMemory mem = new LayeredChatMemory(delegate, svc);
        assertEquals(delegate.messages().size(), mem.messages().size());
    }

    @Test
    void addAndClearDelegate() {
        MessageWindowChatMemory delegate = Mockito.spy(MessageWindowChatMemory.builder().id(1L).maxMessages(100).build());
        MemorySummaryService svc = mock(MemorySummaryService.class);
        LayeredChatMemory mem = new LayeredChatMemory(delegate, svc);

        UserMessage u = UserMessage.from("x");
        mem.add(u);
        mem.clear();
        verify(delegate).add(u);
        verify(delegate).clear();
        assertEquals(1L, mem.id());
    }
}
```

- [ ] **Step 2: 运行验证失败**

Run: `mvn -q test -Dtest=LayeredChatMemoryTest`
Expected: FAIL(`LayeredChatMemory` 不存在)

- [ ] **Step 3: 写实现**

```java
package com.lyw.appgeneration.ai.memory;

import cn.hutool.core.util.StrUtil;
import com.lyw.appgeneration.service.MemorySummaryService;
import dev.langchain4j.data.message.AiMessage;
import dev.langchain4j.data.message.ChatMessage;
import dev.langchain4j.data.message.UserMessage;
import dev.langchain4j.memory.ChatMemory;

import java.util.ArrayList;
import java.util.List;

/**
 * 分层记忆装饰器:包裹 delegate(MessageWindowChatMemory),
 * messages() 返回前前置 L1 摘要(一对 User摘要/AI确认,保证 user/ai 交替)。
 * add/clear/id 全部委托 delegate —— 工具循环、tool 对驱逐、Redis 持久化由 delegate 负责。
 * @author lyw
 */
public class LayeredChatMemory implements ChatMemory {

    private final ChatMemory delegate;
    private final MemorySummaryService summaryService;
    private final Long appId;

    public LayeredChatMemory(ChatMemory delegate, MemorySummaryService summaryService) {
        this.delegate = delegate;
        this.summaryService = summaryService;
        this.appId = (Long) delegate.id();
    }

    @Override
    public Object id() {
        return delegate.id();
    }

    @Override
    public void add(ChatMessage message) {
        delegate.add(message);
    }

    @Override
    public List<ChatMessage> messages() {
        List<ChatMessage> base = delegate.messages();
        String summary = summaryService.getCurrentSummary(appId);
        if (StrUtil.isBlank(summary)) {
            return base;
        }
        List<ChatMessage> result = new ArrayList<>(base.size() + 2);
        result.add(UserMessage.from("以下是本应用早期对话的摘要,供你延续上下文(不是用户的新指令):\n" + summary));
        result.add(AiMessage.from("明白,我会基于以上摘要和后续对话继续。"));
        result.addAll(base);
        return result;
    }

    @Override
    public void clear() {
        delegate.clear();
    }
}
```

- [ ] **Step 4: 运行验证通过**

Run: `mvn -q test -Dtest=LayeredChatMemoryTest`
Expected: PASS(3 个用例)

- [ ] **Step 5: Commit**

```bash
git add src/main/java/com/lyw/appgeneration/ai/memory/LayeredChatMemory.java \
        src/test/java/com/lyw/appgeneration/ai/memory/LayeredChatMemoryTest.java
git commit -m "feat: LayeredChatMemory 装饰器(前置L1摘要,委托delegate)"
```

---

## Task 6: 放宽 `loadChatHistoryToMemory` 签名(冷启动重建)

**Files:**
- Modify: `service/ChatHistoryService.java`(签名 `MessageWindowChatMemory` → `ChatMemory`)
- Modify: `service/impl/ChatHistoryServiceImpl.java`(实现同步)

- [ ] **Step 1: 改接口签名**

`ChatHistoryService.java`:
```java
// 旧: int loadChatHistoryToMemory(Long appId, MessageWindowChatMemory chatMemory, int maxCount);
int loadChatHistoryToMemory(Long appId, dev.langchain4j.memory.ChatMemory chatMemory, int maxCount);
```

- [ ] **Step 2: 改实现参数类型**(逻辑不变,`chatMemory.add(...)` / `chatMemory.clear()` 在 `ChatMemory` 接口上都有)

`ChatHistoryServiceImpl.java:139` 方法签名 `MessageWindowChatMemory` → `ChatMemory`,import 改 `dev.langchain4j.memory.ChatMemory`。方法体不变。

- [ ] **Step 3: 全量编译验证调用方**(确保唯一调用方 `AiGeneratorServiceFactory` 仍兼容——`LayeredChatMemory` 也是 `ChatMemory`)

Run: `mvn -q compile`
Expected: BUILD SUCCESS(若报错,定位其他调用方一并改)

- [ ] **Step 4: Commit**

```bash
git add src/main/java/com/lyw/appgeneration/service/ChatHistoryService.java \
        src/main/java/com/lyw/appgeneration/service/impl/ChatHistoryServiceImpl.java
git commit -m "refactor: loadChatHistoryToMemory 签名放宽为 ChatMemory(支持装饰器)"
```

---

## Task 7: `AiGeneratorServiceFactory` 改造(包裹装饰器)

**Files:**
- Modify: `ai/AiGeneratorServiceFactory.java:74-116`

- [ ] **Step 1: 注入 `MemorySummaryService`**

类字段新增:
```java
@Resource
private com.lyw.appgeneration.service.MemorySummaryService memorySummaryService;
```

- [ ] **Step 2: 用 `LayeredChatMemory` 包裹现有 delegate**

`createAiCodeGeneratorService` 内,把现有构建结果包成装饰器:
```java
MessageWindowChatMemory delegate = MessageWindowChatMemory
        .builder()
        .id(appId)
        .chatMemoryStore(redisChatMemoryStore)
        .maxMessages(100)
        .build();
// 冷启动重建:回填最近原文到 delegate(摘要由 LayeredChatMemory.messages() 注入)
chatHistoryService.loadChatHistoryToMemory(appId, delegate, 20);

// 用装饰器包裹:messages() 前置 L1 摘要
LayeredChatMemory chatMemory = new LayeredChatMemory(delegate, memorySummaryService);
```
后续 `switch` 中 `.chatMemory(chatMemory)` / `.chatMemoryProvider(memoryId -> chatMemory)` 不变(`LayeredChatMemory` 即 `ChatMemory`)。

- [ ] **Step 3: 编译 + 启动冒烟**

Run: `mvn -q compile`
Expected: BUILD SUCCESS
(手动)启动应用,发起一轮 HTML 对话,确认正常流式返回、无消息顺序错误。

- [ ] **Step 4: Commit**

```bash
git add src/main/java/com/lyw/appgeneration/ai/AiGeneratorServiceFactory.java
git commit -m "feat: AiGeneratorServiceFactory 用 LayeredChatMemory 包裹会话记忆"
```

---

## Task 8: 钩子接入(对话结束触发异步提炼)

**关键约束:** `SimpleTextStreamHandler` **不是 Spring Bean**(`StreamHandlerExecutor.java:41` 直接 `new`),不能 `@Resource` 注入。沿用现有模式——像 `chatHistoryService` 那样把 `MemorySummaryService` 作为 `handle()` 参数,由 `StreamHandlerExecutor` 统一透传(executor 自己 `@Resource` 注入)。`doExecute` 对外签名不变,`AppServiceImpl` 无需改动。

**Files:**
- Modify: `core/handler/StreamHandlerExecutor.java`(注入 `MemorySummaryService` + 透传给两个 handle)
- Modify: `core/handler/SimpleTextStreamHandler.java`(`handle()` 加参数 + `doOnComplete` 触发)
- Modify: `core/handler/JsonMessageStreamHandler.java`(`handle()` 加参数 + `doOnComplete` 触发)

- [ ] **Step 1: `StreamHandlerExecutor` 注入并透传**

新增注入字段:
```java
@Resource
private com.lyw.appgeneration.service.MemorySummaryService memorySummaryService;
```
`doExecute` 内两处 handle 调用末尾加 `memorySummaryService` 参数:
```java
case VUE_PROJECT ->
        jsonMessageStreamHandler.handle(originFlux, chatHistoryService, appId, loginUser, memorySummaryService);
case HTML, MULTI_FILE ->
        new SimpleTextStreamHandler().handle(originFlux, chatHistoryService, appId, loginUser, memorySummaryService);
```

- [ ] **Step 2: `SimpleTextStreamHandler.handle()` 加参数 + 触发**

`handle(...)` 签名末尾加 `MemorySummaryService memorySummaryService`;在 `addChatMessage(AI...)` 之后追加触发:
```java
.doOnComplete(() -> {
    String aiResponse = aiResponseBuilder.toString();
    chatHistoryService.addChatMessage(appId, aiResponse, ChatHistoryMessageTypeEnum.AI.getValue(), loginUser.getId());
    memorySummaryService.triggerSummarizationAsync(appId);  // 异步 best-effort,不阻塞流返回
})
```

- [ ] **Step 3: `JsonMessageStreamHandler.handle()` 加参数 + 触发**

`handle(...)` 签名末尾加 `MemorySummaryService memorySummaryService`;在其 `addChatMessage(AI...)` 之后追加 `memorySummaryService.triggerSummarizationAsync(appId);`(不影响既有 Vue 构建逻辑)。
> `JsonMessageStreamHandler` 虽是 `@Component` 本可 `@Resource` 注入,但为与 `SimpleTextStreamHandler` 保持一致的传参风格,统一走 `handle()` 参数。

- [ ] **Step 4: 编译**

Run: `mvn -q compile`
Expected: BUILD SUCCESS

- [ ] **Step 5: Commit**

```bash
git add src/main/java/com/lyw/appgeneration/core/handler/StreamHandlerExecutor.java \
        src/main/java/com/lyw/appgeneration/core/handler/SimpleTextStreamHandler.java \
        src/main/java/com/lyw/appgeneration/core/handler/JsonMessageStreamHandler.java
git commit -m "feat: 对话结束钩子触发异步L1摘要提炼(executor统一透传依赖)"
```

---

## Task 9: 集成测试 + 端到端验证

**Files:**
- Test: `src/test/java/com/lyw/appgeneration/ai/memory/LayeredMemoryIntegrationTest.java`(`@SpringBootTest`,可选 mock 模型)

- [ ] **Step 1: 写集成测试**——模拟"长对话 → 触发摘要 → 冷启动重建带摘要"

```java
// 要点(伪代码骨架,按项目测试基建补全):
// 1. 准备 appId,写入 >MIN_NEW_MESSAGES_TO_SUMMARIZE 条 chat_history
// 2. 调 memorySummaryService.summarizeNow(appId)(同步),断言 app_memory_summary 有行、游标推进
// 3. 新建 delegate(空)+ loadChatHistoryToMemory 回填最近20条 + LayeredChatMemory 包裹
// 4. 断言 mem.messages() 第0条是含摘要的 UserMessage、后续是回填原文,且无连续同角色
```

- [ ] **Step 2: 运行**

Run: `mvn -q test -Dtest=LayeredMemoryIntegrationTest`
Expected: PASS

- [ ] **Step 3: 手动端到端**——同一 app 连续对话 >8 轮触发摘要;重启应用(清 Redis)后继续对话,确认 AI 仍"记得"早期需求(摘要生效)。

- [ ] **Step 4: 全量回归**

Run: `mvn -q test`
Expected: 全绿(含既有 `JsonMessageStreamHandlerTest` 等)

- [ ] **Step 5: Commit**

```bash
git add src/test/java/com/lyw/appgeneration/ai/memory/LayeredMemoryIntegrationTest.java
git commit -m "test: 分层记忆一期集成测试(摘要触发+冷启动重建)"
```

---

## 验收标准(一期)

- [ ] 长对话超窗口后,早期需求/偏好不丢(由 L1 摘要保留)——手动端到端验证。
- [ ] 重启/Redis 失效后,`messages()` = 摘要 + 最近原文,上下文不断裂。
- [ ] 摘要提炼全程异步,不阻塞流式首字。
- [ ] 模型失败/线程池满/读 MySQL 失败 → 降级不影响主对话(单测覆盖)。
- [ ] 既有 HTML / MULTI_FILE / VUE_PROJECT 三条生成链路回归通过。

## 开放项 / 实现期决定

- 摘要触发阈值(`MIN_NEW_MESSAGES_TO_SUMMARIZE`)、单次并入条数、窗口大小——压测校准。
- 是否新建独立 `summarizationChatModel` Bean(隔离 maxTokens/temperature)vs 复用 `openAiChatModel`。
- `messages()` 每轮读 MySQL 摘要的 DB 压力——必要时加 Redis 缓存(摘要更新失效)。
- 游标用 `id` 的单调性假设;多写库场景改 `createTime`+`id`。
